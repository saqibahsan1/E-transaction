<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.android.makeyousmile">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.OrdersActivity"
            android:label="Orders"></activity>
        <activity android:name=".ui.activity.DonationActivity"
            android:label="Donation"/>
        <activity android:name=".ui.activity.RestaurantActivity"
            android:label="Restaurant"/>
        <activity android:name=".ui.activity.DeliveryBoyActivity"
            android:label="DeliveryBoy"/>
        <activity android:name=".ui.activity.OrganizationActivity"
            android:label="Organization"/>
        <activity
            android:name=".ui.Activities.Splash"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.Activities.MainActivity" />

        <activity android:name=".ui.Activities.Login"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

    </application>

    <dist:module dist:instant="true" />

</manifest>